/*
import com.google.common.collect.ArrayListMultimap;
import com.google.common.collect.ListMultimap;
*/

public class MultimapExample {
    public static void main(String[] args) {
       /* ListMultimap<String, Integer> multimap = ArrayListMultimap.create();

        // Add some key-value pairs to the multimap
        multimap.put("fruit", 1);
        multimap.put("fruit", 2);
        multimap.put("fruit", 3);
        multimap.put("vegetable", 4);
        multimap.put("vegetable", 5);

        // Get all values associated with the "fruit" key
        System.out.println("Fruit: " + multimap.get("fruit"));

        // Get all values associated with the "vegetable" key
        System.out.println("Vegetable: " + multimap.get("vegetable"));*/

    }
}
